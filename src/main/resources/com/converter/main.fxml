<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import java.lang.String?>

<VBox spacing="20.0" prefWidth="550.0" xmlns="http://javafx.com" xmlns:fx="http://javafx.com">
    <children>
        <!-- Заголовок: используем встроенный класс title-2 -->
        <Label text="Finstore to Snowball Converter">
            <styleClass>
                <String fx:value="title-2" />
            </styleClass>
        </Label>

        <!-- Зона 1: MHTML (стиль интерактивной панели) -->
        <VBox alignment="CENTER" spacing="10" prefHeight="120"
              style="-fx-border-color: -color-border-default; -fx-border-width: 1; -fx-border-style: dashed; -fx-border-radius: 6;">
            <children>
                <Label text="📂 Перетащите .mhtml файлы сюда" styleClass="text-muted" />
                <Button text="Выбрать файлы" styleClass="button-outlined" />
            </children>
            <padding><Insets bottom="20" left="20" right="20" top="20" /></padding>
        </VBox>

        <!-- Чекбокс Категорий -->
        <CheckBox fx:id="addCategoriesCheck" text="Добавить категории к существующим" />

        <!-- Зона 2: CSV -->
        <VBox fx:id="csvDropZone" disable="${!addCategoriesCheck.selected}"
              alignment="CENTER" spacing="10" prefHeight="80"
              style="-fx-border-color: -color-border-muted; -fx-border-width: 1; -fx-border-style: dashed; -fx-border-radius: 6;">
            <children>
                <Label text="📄 Файл категорий (.csv)" styleClass="text-muted" />
            </children>
        </VBox>

        <Separator />

        <!-- Выбор отчетов -->
        <HBox spacing="15">
            <CheckBox text="XLSX Report" selected="true" />
            <CheckBox text="Snowball XLSX" selected="true" />
            <CheckBox text="Snowball CSV" selected="true" />
        </HBox>

        <Region VBox.vgrow="ALWAYS" /> <!-- Распорка -->

        <!-- Прогресс и Кнопка -->
        <ProgressBar progress="0.0" maxWidth="Infinity" />

        <!-- Кнопка: используем успех (зеленая) и большой размер -->
        <Button text="КОНВЕРТИРОВАТЬ И СКАЧАТЬ .ZIP" maxWidth="Infinity" prefHeight="45">
            <styleClass>
                <String fx:value="success" />
                <String fx:value="large" />
            </styleClass>
        </Button>
    </children>
    <padding><Insets bottom="25.0" left="25.0" right="25.0" top="25.0" /></padding>
</VBox>
